# Example Project AGENTS.md

@~/AGENTS.md

## Project Notes

Before work: read global Synapse instructions + `$BRAIN_VAULT/index.md` (Phase 0 per workflow).

After meaningful work: distill into the vault, update `index.md` / `hot.md` / `log.md`,
run `synapse check`.
